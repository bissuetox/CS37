#include <iostream>
#include <iomanip>
#include <string>
using namespace std;

void inputData(***);
void printResults(***);

int main()
{
	string cropName;
	float cpAcre[4], ppBushel[4], increase[4];
	int yield[4];
	for (int i = 0; i < 4; i++) {
		-call global function to get data for a crop
			- call global function to perform calculations for the crop
			- call global function to display results for the crop
			- keep track of best crop
	}
		cout << endl << "Old MacDonald, you should plant " << bestCrop << endl << endl;
}


void calculate(***)
{
	int NEWSHITNIGGA;
}


void inputData(***)
{
	cout << "Enter the crop name: ";
	getline(cin, cropName);
	cout << "Enter cost, yield, price per bushel, and increase data: ";
	cin >> cpAcre >> yield >> ppBushel >> increase;
	
}


void printResults(***)
{

}

/*
  Copy the results of your program here.
  Replace the '***' with the appropriate parameter list for each function
  Remove all unused code
*/